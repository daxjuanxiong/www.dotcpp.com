#include <iostream>

int main() {
    // 定义输入的整数
    int a, b, c;

    // 输入三个整数
    std::cin >> a >> b >> c;

    // 计算表达式的值
    int result = (a + b) * c;

    // 输出结果
    std::cout << result << std::endl;

    return 0;
}
