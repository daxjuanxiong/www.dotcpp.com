#include <iostream>

int main() {
    // 定义两个自然数变量
    int x, y;

    // 输入两个自然数
    std::cin >> x >> y;

    // 计算它们的和
    int sum = x + y;

    // 输出结果
    std::cout << sum << std::endl;

    return 0;
}
